{
  "definitions": [
    {
      "title": "Gated Form",
      "id": "gated-form",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Gated Form",
              "model": "gated-form"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "gated-form",
      "fields": [
        {
          "component": "text",
          "name": "formId",
          "type": "string",
          "label": "Form ID",
          "required": true,
          "description": "Unique identifier for the gated form."
        },
        {
          "component": "text",
          "name": "formName",
          "type": "string",
          "label": "Form Name",
          "required": true,
          "description": "Name for the gated form."
        },
        {
          "component": "text",
          "name": "_clientID",
          "type": "string",
          "label": "Client ID",
          "required": true,
          "description": "Salesforce Marketing Cloud Client ID."
        },
        {
          "component": "text",
          "name": "_deExternalKey",
          "type": "string",
          "label": "Data Extension External Key",
          "required": true,
          "description": "Salesforce Marketing Cloud Data Extension External Key."
        },
        {
          "component": "text",
          "name": "_action",
          "type": "string",
          "label": "Action",
          "required": true,
          "default": "add",
          "description": "Form submission action (add/update)."
        },
        {
          "component": "text",
          "name": "_returnXML",
          "type": "string",
          "label": "Return XML",
          "required": false,
          "description": "Return format for the form submission."
        },
        {
          "component": "text",
          "name": "Inquiry_Type",
          "type": "string",
          "label": "Inquiry Type",
          "required": true,
          "default": "Gated Content",
          "description": "Type of inquiry for tracking purposes."
        },
        {
          "component": "text",
          "name": "Form_Type",
          "type": "string",
          "label": "Form Type",
          "required": true,
          "default": "gated",
          "description": "Category of the form."
        },
        {
          "component": "text",
          "name": "_successURL",
          "type": "string",
          "label": "Success URL",
          "required": true,
          "description": "URL to redirect to after successful form submission."
        },
        {
          "component": "text",
          "name": "_errorURL",
          "type": "string",
          "label": "Error URL",
          "required": false,
          "description": "URL to redirect to if form submission fails."
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "gated-form",
      "components": [
        "gated-form"
      ]
    }
  ]
}